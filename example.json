{
  "DefaultBranch": "",
  "Projects": [
    {
      "Path": "",
      "PublishPath": "",
      "Type": "", // API, Blazor, Angular, Nuget, Console,
      "Name": "", // Opcional
      "Scripts": { // Opcional, são scripts executados apenas no projeto configurado
        "Before": [
          {
            "Type": "", // powershell (quando é inline), ps1 (scripts prontos), cmd (quando é comando)
            "Path": "", // Script que está em local diferente (deve ser resolvido o local pois pode ou não ser relativo)
            "Command": "", // Quando é script inline
          }
        ],
        "After": [
          {
            "Type": "", // powershell (quando é inline), ps1 (scripts prontos), cmd (quando é comando)
            "Path": "", // Script que está em local diferente (deve ser resolvido o local pois pode ou não ser relativo)
            "Command": "", // Quando é script inline
          }
        ],
      }
    }
  ],
  "Scripts": { // Opcional, são scripts executados independente de projeto
    "Before": [
      {
        "Type": "", // powershell (quando é inline), ps1 (scripts prontos), cmd (quando é comando)
        "Path": "", // Script que está em local diferente (deve ser resolvido o local pois pode ou não ser relativo)
        "Command": "", // Quando é script inline
      }
    ],
    "After": [
      {
        "Type": "", // powershell (quando é inline), ps1 (scripts prontos), cmd (quando é comando)
        "Path": "", // Script que está em local diferente (deve ser resolvido o local pois pode ou não ser relativo)
        "Command": "", // Quando é script inline
      }
    ],
  }
}
